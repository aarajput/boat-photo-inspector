import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seasure.boatphotos',
  appName: 'Boat Photo Inspector',
  webDir: 'dist'
};

export default config;
